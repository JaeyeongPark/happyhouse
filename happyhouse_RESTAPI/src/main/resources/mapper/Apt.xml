<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.dao.AptDAO">

	<select id="aptlist" resultType="AptDTO"  parameterType="map">
		select aptName, hi.lat, hi.lng, dealAmount, dealYear, dealMonth, dealDay, area
		from baseaddress b join houseinfo hi
		on (b.dongcode = hi.dongcode)
		join housedeal hd
		on (hi.aptCode = hd.aptCode)
		where b.sidoName = #{AddressDTO.sidoName}
		and b.gugunName = #{AddressDTO.gugunName}
		and b.dongName = #{AddressDTO.dongName}
		<if test='listParameterDTO.word != null'>
			<if test='listParameterDTO.key.equals("aptName")'>
			and hi.aptName like concat('%', #{listParameterDTO.word}, '%')
			</if>
		</if>
		limit #{listParameterDTO.start},#{listParameterDTO.currentPerPage}
		
	</select>
	
	<select id="stationlist" resultType="StationDTO">
		select line, name, code, lat, lng
		from station
	</select>

	<select id="getTotalCount" resultType="int" parameterType="map">
		select count(aptName)
		from baseaddress b join houseinfo hi 
		on (b.dongcode = hi.dongcode)
		join housedeal hd
		on (hi.aptCode = hd.aptCode)
		where b.sidoName = #{AddressDTO.sidoName}
		and b.gugunName = #{AddressDTO.gugunName}
		and b.dongName = #{AddressDTO.dongName}
		<if test='listParameterDTO.word != null'>
			<if test='listParameterDTO.key == "aptName"'>
			and hi.aptName like concat('%', #{listParameterDTO.word}, '%')
			</if>
			<if test='listParameterDTO.key != "aptName"'>
			and #{listParameterDTO.key} = #{listParameterDTO.word}
			</if>
		</if>
	</select>
	

</mapper>

